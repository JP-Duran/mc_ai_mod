package dwarf.entity.custom.findOres.structures;

public class AStar {

    /*
    call astar alg
    returns a BlockPath

    MEMBERS
    EnvironmentScan env


    NOTES
    Need path from every ore to every other ore to build weighted graph to solve TSP
    Graph node:
        coordinate start
        coordinate end
        length
        list<BlockPos>



    AStar(World world, BlockPos centerPos, int scanRadius)

    list<BlockPos> AStarPath(int startX, int startY, int startZ, int endX, int endY, int endZ)
     */
}
